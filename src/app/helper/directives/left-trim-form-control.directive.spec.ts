import { LeftTrimFormControlDirective } from './left-trim-form-control.directive';

describe('LeftTrimFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftTrimFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
